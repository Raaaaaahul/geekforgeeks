#include<bits/stdc++.h>
using namespace std;
void solve(vector<int>& v,)
int main()
{
    int size;
    cin>>size;
    vector<int> v(size);
    for(int i=0;i<size;i++)
    {
        cin>>v[i];
    }
    solve(v,size);
    return 0;
}